language: java
sudo: false

jdk:
- oraclejdk8

addons:
  sonarcloud:
    token:
      secure: ${SONARCLOUD_TOKEN}
      
script:
  - mvn clean sonar:sonar

cache:
  directories:
  - "$HOME/.m2"
  - '$HOME/.sonar/cache'
  
